# torch is not pinned here: install it first from the matching CUDA wheel
# index for your system, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# torch-geometric is then installed on top of that torch build.
#
# FEniCSx is a separate dependency, installed in its own WSL2 conda
# environment — see the README's Environment section for setup.

torch-geometric==2.7.0
numpy==2.4.3
scipy==1.16.3
scikit-learn==1.8.0
matplotlib==3.10.7
pandas==2.3.3
jupyter==1.1.1
pyyaml==6.0.3
tqdm==4.67.1
